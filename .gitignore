node_modules/
dist/
.env
.env.*
!.env.example
runs/*.jsonl
!runs/.gitkeep
.mcp.log
.gstack/

# Generated by runs, not source. The map, the corpus, the traces and the
# screenshots are all outputs -- they are reproducible from the code and the
# tasks, and committing them makes every run a diff.
data/*.jsonl
data/*.json
data/*.png
*.png
runs/
adapters/
