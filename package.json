{
  "name": "placebo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An agent that has to prove its fix caused the fix. Causal Runtime Alignment for Roblox game repair, orchestrated by the TrueForge agent harness.",
  "license": "MIT",
  "engines": {
    "node": ">=22.14"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "verify": "tsx src/verifier/cli.ts",
    "build:game": "tsx src/verifier/buildGame.ts",
    "make:game": "tsx src/orchestrator/makeGame.ts",
    "mcp": "tsx src/mcp/server.ts",
    "run:agent": "tsx src/orchestrator/main.ts",
    "probe:studio": "tsx scripts/probe-studio.ts",
    "probe:restore": "tsx scripts/probe-restore-fidelity.ts",
    "probe:worldstate": "tsx scripts/probe-worldstate.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@truefoundry/trueforge-sdk": "^0.1.3",
    "cheerio": "^1.2.0",
    "express": "^5.1.0",
    "yaml": "^2.8.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^22.15.3",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
